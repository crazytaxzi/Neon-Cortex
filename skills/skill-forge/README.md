# Skill Forge

This is the reusable Neon Cortex skill definition for creating and validating
other skills. The implementation lives in the top-level `skillforge` Python
package.

```powershell
python -m skillforge create "Example Skill" --description "Demonstrates the format"
python -m skillforge validate skills/example-skill
```
